# AGENTS.md

## 适用范围

本文件适用于整个 ZTools vNext 仓库。它规定开发代理的工作方式，不是产品设计本身，也不取代 `docs/` 与 ADR。

当前仓库处于 Design Baseline 0.1 阶段。在路线图明确进入实现阶段前，不得自行创建工程骨架、引入依赖或编写业务代码。

## 开始任务前

1. 阅读 `README.md`，确认仓库当前阶段。
2. 阅读与任务直接相关的 `docs/` 文档。
3. 检查 `docs/adr/README.md` 及相关 ADR 的状态。
4. 检查 `docs/OPEN_ITEMS.md`，确认任务没有跨过当前 Gate 的阻断项。
5. 按 `docs/DESIGN_PROCESS.md` 判断变更等级。
6. 按 `docs/TESTING.md` 确认对应测试层级、平台证据和发布门禁。
7. 阅读 `docs/MAINTAINER_COMMUNICATION.md`。日常技术决定由 agent 负责；只有产品方向、费用、外部账号/服务、扩大权限或不可逆影响需要维护者决定。

## 设计权威与冲突处理

- `docs/PRINCIPLES.md` 保存长期原则。
- 状态为 `accepted` 的 ADR 保存已经批准且不可静默改写的决策历史。
- 其余主题文档描述当前目标设计；状态为 `draft` 的内容不能视为承诺。
- `AGENTS.md` 只负责流程、约束和文档路由，不得成为另一份架构说明书。
- 文档、ADR、测试或实现发生冲突时，不得选择最方便的一方继续开发；应按 `docs/DESIGN_PROCESS.md` 识别权威、记录差异并完成设计变更。
- Gate（工程阶段）转换必须在 `docs/reviews/` 中留下明确评审记录，不得从路线图、提交或 CI 状态自行推断。纯技术阶段可由获得委托的开发 agent 根据证据决定；涉及产品方向、公开支持承诺、费用、外部账号/服务、扩大权限或不可逆影响时必须由维护者决定。
- `docs/OPEN_ITEMS.md` 中设计状态与执行进度必须分别更新；不得用 `accepted` 表示验证已完成，也不得用代码存在倒推设计已批准。

## 维护者沟通与授权

- `docs/MAINTAINER_COMMUNICATION.md` 是所有维护者审批请求的强制格式与范围规则。
- 维护者已把既定产品边界内的技术决策权委托给开发 agent。架构、版本、接口、内部协议、测试和实现细节由 agent 自主决定、记录并验证，不要求维护者理解或批准专业文档。
- 只有改变产品方向、产生或可能产生费用、操作外部账号/服务、扩大权限或造成不可逆影响时才暂停并询问维护者。
- 请求必须先用普通中文说明需要决定什么、为什么、推荐方案，以及产品、费用、外部服务、权限和不决定的影响；不得只请求批准 ADR、Gate、baseline 或其他术语。
- 若一项 Level 2/3 变更完全属于技术实现且不触发上述维护者审批范围，agent 可以依据委托权完成 ADR、评审和状态变更；降低既有安全/隐私承诺、改变平台或兼容承诺属于产品方向，必须询问。
- 进度知会不是审批。不得把技术责任转嫁给维护者，也不得把维护者沉默视为批准。

## 强制架构边界

- 领域核心不得依赖 Electron、Vue、Node.js 平台 API、具体数据库或操作系统实现。
- 平台差异必须通过 Capability 契约与 Adapter 隔离，禁止在业务层散布 `process.platform`。
- 跨进程调用必须使用类型化契约，并声明调用者角色、输入输出 Schema、权限、超时、取消语义、错误和审计要求。
- 未知调用者、未知方法、无效输入和权限不足必须默认拒绝。
- 插件不得直接访问 Node.js、Electron、裸 IPC 或宿主私有对象，也不得提供自定义 Electron preload。
- 插件 UI 与后台 Worker 必须拥有独立生命周期、权限上下文和资源配额。
- 不得通过关闭 `contextIsolation`、`sandbox` 或 `webSecurity` 解决兼容问题。
- 宿主搜索、设置、市场、账号和权限中心属于可信 Host UI，不得借“内置插件”绕过边界。

## 平台与旧项目参考规则

- Windows、macOS 和 Ubuntu 26.04 GNOME Wayland 都是一等设计目标；能力允许按平台受控降级。
- Wayland 能力必须遵循 Portal、D-Bus 与桌面环境授权机制，不得绕过用户同意或系统安全模型。
- 旧仓库 `/home/void/work/projects/ZTools` 只读参考。默认只提取用户可见行为、平台边界、失败案例、数据兼容需求、测试场景和输入输出明确的纯算法。
- 默认禁止复制旧项目的大型 Manager、preload、裸 IPC 注册、插件特权模型、全局单例互调、平台条件分支和关闭浏览器安全的配置。
- 任何从旧项目迁入的代码都必须给出独立理由，并符合 vNext 当前文档；“旧项目就是这样做的”不构成理由。

## 实现与验证规则

- 优先完成小型纵向切片；每个切片必须同时覆盖契约、权限、失败路径、测试和必要文档。
- 新增或实质修改的方法、函数，必须在声明正上方使用多行 JSDoc 说明职责，并为每个参数提供 `@param`、提供 `@returns`，可能主动抛错时补充 `@throws`。
- 在校验、状态切换、资源发布、回滚和清理等关键步骤前添加解释目的或失败边界的单行注释，禁止逐行复述代码。
- 不得以类型断言、关闭检查、扩大权限或吞掉错误来绕过契约。
- 测试必须使用隔离的数据目录和插件目录，不得读写用户真实数据。
- 修改实现时同步检查设计文档；只有设计真实变化时才修改文档，禁止为了让冲突消失而改写历史 ADR。
- 完成任务前按 `docs/TESTING.md` 运行与改动相称的类型检查、单元测试、契约测试、架构边界检查和端到端测试；不能运行的项目及缺少的真实平台证据必须明确报告。

## 提交前检查

- 变更是否符合已接受原则和 ADR。
- 是否新增了未声明的跨层依赖、IPC、权限或平台假设。
- 是否为不支持的能力提供了显式状态与降级体验。
- 是否记录了安全、隐私、数据所有权和资源释放边界。
- 是否更新了真正受影响的文档、测试和状态标记。
